{
  "name": "miicase-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "miiCase Cloud Run API tier — Firebase auth + session-variable RLS over Cloud SQL",
  "scripts": {
    "start": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types test/api.test.ts"
  },
  "engines": { "node": ">=22.6" },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "optionalDependencies": {
    "firebase-admin": "^12.0.0"
  }
}
